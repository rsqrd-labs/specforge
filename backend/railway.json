{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "alembic upgrade head && gunicorn main:app -c gunicorn.conf.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
