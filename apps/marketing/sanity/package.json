{
  "name": "specforge-studio",
  "private": true,
  "version": "0.1.0",
  "description": "Sanity Studio for the SpecForge marketing zone (issue #18, Phase 3). Standalone editor surface — deployed separately (sanity deploy), never embedded on the public marketing origin.",
  "type": "module",
  "scripts": {
    "dev": "sanity dev",
    "start": "sanity start",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sanity/vision": "^3.68.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "sanity": "^3.68.0",
    "styled-components": "^6.1.13"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "typescript": "^5.6.3"
  }
}
