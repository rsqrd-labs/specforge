# Thought2Build marketing studio environment (issue #18, Phase 3).
# Public project values — safe to expose; the studio authenticates editors via
# their own Sanity login, not these. No secrets belong in this file.
SANITY_STUDIO_PROJECT_ID=your_project_id
SANITY_STUDIO_DATASET=production
