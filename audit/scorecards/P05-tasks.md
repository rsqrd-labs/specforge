# P05 — `backend/prompts/tasks.py` (Tasks stage, core generation 4/4)

Shares P01 infra + P02-P04 structural pattern. Focus on what's distinct.

| # | Dimension | Score | Evidence / justification |
|---|-----------|-------|---------------------------|
| 1 | Task clarity & specificity | 5 | The exact task-block format (`tasks.py:50-90`) is field-by-field specified with allowed enum values for Priority/Estimate/Risk — no ambiguity in shape. |
| 2 | Role & context framing | 5 | "principal engineering lead" producing "the implementation playbook where an agent or engineer executes each task with no extra context" (`tasks.py:16-18`) — correctly frames the reader as an executor, which shapes the "concrete file-level actions" requirement that follows. |
| 3 | Structure & organization | 5 | Same canonical ordering; phase-ordering guidance (`tasks.py:44-46`) gives a sensible default sequence while allowing deviation "as the product requires." |
| 4 | Output format specification | 5 | The Effort Summary's exact 4-line format (`tasks.py:33-38`) is machine-parseable and is exactly what feeds the frontend's effort/coverage panels (per CLAUDE.md's `CoveragePanel`/`TaskValidationPanel` description) — verified the label strings (`Estimate range:`, `Tasks: N total`, `Sizes:`, `Minimum cut:`) are the literal format spelled out in the prompt. |
| 5 | Examples & demonstrations | 5 | The single worked task example (`tasks.py:156-201`) is the most complete example in the repo — full field set, realistic Steps/Acceptance Criteria/Rollback — a strong single-shot anchor for a 12-field template. |
| 6 | Edge case & failure handling | 4 | Setup-only tasks get an explicit escape hatch (`_(none — <reason>)_`, `tasks.py:56,96`) so validators can distinguish "correctly has no test" from "generation forgot the field" — same good pattern as harness.py's `TestCategoryGap`. Gap: no explicit instruction for what to do if the harness itself is incomplete/has a gap record — tasks.py doesn't mention `TestCategoryGap` at all, so a harness that recorded a deferred category has no defined tasks-stage behavior (should it still reference the gap? Skip it silently? Undefined). |
| 7 | Constraint completeness | 5 | The three mandatory dependency-introducing-task acceptance criteria (SCA exit-0, version-pin match, non-deprecated status — `tasks.py:107-111`) directly close the loop with plan.py's Technology Stack table and its Support-status enum — genuinely well cross-referenced. |
| 8 | Injection & untrusted-content safety | 5 | Same wrap + explicit-ignore pattern, applied to all three dependencies (`tasks.py:129-131, 203-204`). |
| 9 | Variable interpolation safety | 5 | Three wrapped dependencies, no bespoke logic. |
| 10 | Consistency with target model | 4 | Same unverified per-provider concern as siblings. |
| 11 | Token & cost efficiency | 4 | Reasonable; one nit — `# nosec B608` suppressions appear twice (`tasks.py:118, 221`) on lines that are plain triple-quoted prompt text, not SQL (`B608` is a Bandit SQL-injection rule) — these look like copy-pasted suppressions from elsewhere that don't correspond to any actual bandit finding on these lines (the string content is prose, not a query). Harmless but suggests either a stale suppression or bandit false-positiving on the literal word patterns in the prompt text; worth a one-time check of whether removing it still passes `ruff`/`bandit` clean (CLAUDE.md's CI gate). |
| 12 | Determinism & robustness | 3 | Same generic ding, plus: the Effort Summary is explicitly "derived after the full task list" (`tasks.py:33`) and must stay "consistent with the per-task Priority and Estimate fields" (`tasks.py:38|215`) — a cross-artifact-consistency requirement that's easy for a cheap model to get subtly wrong (e.g., off-by-one task count) with no in-prompt self-check beyond the verify list; this is exactly the kind of thing a zero-LLM validator could catch deterministically post-hoc, and CLAUDE.md doesn't describe one for Effort Summary math. |
| 13 | Maintainability | 5 | Versioned (`tasks-v4`), Langfuse-overridable, exact string contracts shared with `online_eval.py` (`_PRIORITY_ENUM`, `_ESTIMATE_ENUM`, `_TASK_HEADING_RE` — verified these regexes match the prompt's stated format). |
| 14 | Testability & eval coverage | 4 | `online_eval.py` has extensive structural parsing (`_parse_task_blocks`, harness-ref extraction, dropped-category reclassification) that doubles as an implicit contract test for this prompt's format — strong. No dedicated deterministic check (in `harness/prompt_eval/graders/`) for Effort Summary count-consistency (dimension 12's gap) at generation time, only at eval-judge time (which is advisory, not blocking). |

**Top risk:** No defined behavior when the harness stage recorded a `TestCategoryGap` — the tasks prompt has no instruction to check for or react to that record, so a task list can silently omit any acknowledgement of known-deferred coverage the harness stage flagged, breaking the "no plan artifact is orphaned" promise (`tasks.py:138`) for exactly the artifacts most likely to need explicit handling.

**Verdict:** EXEMPLARY (dimension 6/12 gaps noted; both are good remediation-plan candidates — teach tasks.py about `TestCategoryGap`, and add a deterministic Effort-Summary-consistency check).
