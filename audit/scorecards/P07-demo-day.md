# P07 — `backend/prompts/demo_day.py` (4 stage variants, Demo Day mode)

| # | Dimension | Score | Evidence |
|---|-----------|-------|----------|
| 1 | Task clarity & specificity | 5 | The two-promises framing (construction guarantee vs. advisory time budget, `demo_day.py:36-45`) is an unusually clear articulation of what the artifact does and does NOT certify — prevents the model (and, per the file's own docstring, the user) from over-trusting the minute estimates. |
| 2 | Role & context framing | 5 | Each of the 4 roles (`_SPEC_ROLE`, `_PLAN_ROLE`, `_HARNESS_ROLE`, `_TASKS_ROLE`) is scoped correctly and cross-references the parse-stable identifier contract (`demo_day.py:73-87`) that the zero-LLM linter (`demo_day_plan_linter.py`) depends on — good, verified cross-file consistency (the linter's regexes for `### T-NNN:`, `Precondition:`, `Harness refs:` match the literal tokens the prompt mandates). |
| 3 | Structure & organization | 5 | Same canonical ordering, adapted (`_demo_day_system_prompt`, `demo_day.py:105-114`) to insert the Demo Day directive block after the output-rules block rather than the standard prompts' role/structure block — a deliberate, documented reordering, not drift. |
| 4 | Output format specification | 5 | The parse-stable identifier contract is the standout: `demo_day.py:73-87` enumerates the EXACT tokens (`FR-NNN`, `AC-NNN`, `### T-NNN:`, `Precondition:`, `Harness refs:`) a downstream zero-LLM verifier joins on, with an explicit warning that "without those tokens the verifier silently cannot match and every package reads as 'gaps'" (`demo_day.py:11`) — rare and valuable: the prompt author documented the exact failure mode of under-specifying format. |
| 5 | Examples & demonstrations | 3 | Zero worked examples across all 4 roles and all 4 user-prompt builders — a real gap relative to P02-P05, especially given the parse-stable contract above is *more* format-sensitive (a linter joins on exact tokens) than the standard stages, where a missed ID mostly costs traceability rather than triggering a hard "gaps" verdict. |
| 6 | Edge case & failure handling | 4 | "If AI is core, name the model... if it is not used, say so plainly" (`demo_day.py:145-146`) gives a defined branch for the AI Usage rubric section. No equivalent defined behavior for a product idea that genuinely cannot be scoped to one happy path in ~5 hours (e.g., an inherently multi-actor workflow) — the prompt assumes ruthless scoping is always possible. |
| 7 | Constraint completeness | 5 | "Anti-gimmick honesty" (`demo_day.py:68-71`) is a genuinely uncommon and well-targeted constraint — explicitly forbidding the shallow, ungrounded rubric-answer failure mode ("never overclaim, never answer in one vague sentence") that a lesser prompt would leave implicit. |
| 8 | Injection & untrusted-content safety | 5 | Same wrap + explicit-ignore pattern as P02-P05, applied per stage (`demo_day.py:301,312,329,337,357,367,388,402`). |
| 9 | Variable interpolation safety | 5 | Same as standard stages — no bespoke logic beyond the shared `dependencies` dict contract. |
| 10 | Consistency with target model | 4 | Same unverified per-provider concern; slightly higher stakes here since CLAUDE.md states Demo Day is "mid-tier" biased specifically *because* the v1 lean prompts produced shallow output on the cheap tier — i.e., this family already has a documented history of tier-sensitivity, which is direct evidence for the general P02-P05 Dimension-10 concern, not just a theoretical one. |
| 11 | Token & cost efficiency | 3 | `DEMO_DAY_OUTPUT_RULES` (`demo_day.py:89-102`) is a near-duplicate of `PROFESSIONAL_OUTPUT_RULES` (P01) — both instruct "produce only the requested artifact," "evidence over adjectives," stable terminology. The file's own docstring doesn't explain *why* a parallel copy exists rather than composing the shared one with a Demo Day addendum; the practical effect is two English descriptions of the same constraint that must be kept in sync by hand if either output-rules text is revised. |
| 12 | Determinism & robustness | 3 | Same generic ding, compounded by the "single happy path, ruthlessly scoped" instruction being inherently more subjective (what counts as "one" path?) than the standard stages' more mechanical section-by-section contract — more room for run-to-run scope drift. |
| 13 | Maintainability | 4 | Versioned separately (`DEMO_DAY_PROMPT_VERSION`, P01) with an explicit non-mutation guarantee for the standard prompts (`demo_day.py:15`) — a clean regression-pin discipline. The `DEMO_DAY_OUTPUT_RULES` duplication (dimension 11) is the one maintainability wart. |
| 14 | Testability & eval coverage | 4 | `docs/evals/golden_prompts/demo_day_route_golden.json` exists and `demo_day_verdict.py`/`demo_day_plan_linter.py` provide strong zero-LLM structural regression coverage for the parse-stable contract. No example-driven eval catches the "zero worked examples" gap (dimension 5) — coverage exists for *whether* the contract's tokens appear, not for whether smaller models reliably produce them without an example to anchor format. |

**Top risk:** The parse-stable identifier contract is the single highest-stakes format requirement in the repo (a miss reads as "every package has gaps," per the file's own docstring) and is also the one enforced with zero worked examples across any of the 4 roles — the dimension-4 strength and dimension-5 weakness sit on the same feature.

**Verdict:** HIGH (very strong constraint-writing; recommend adding at least one compact worked example per role showing the exact token shapes, given the documented cost of getting them wrong).
