# P09 — `backend/prompts/spec_clarification.py` (pre-spec clarifying-questions judge)

| # | Dimension | Score | Evidence |
|---|-----------|-------|----------|
| 1 | Task clarity & specificity | 5 | "3-5 most decision-shaping questions" with a concrete exclusion rule ("skip generic questions... whose answer would not change a section of the spec", `spec_clarification.py:31-32`) — sharply scoped. |
| 2 | Role & context framing | 4 | "senior product clarification agent" (`spec_clarification.py:23`) is fine, but this is the only judge prompt in the family with no explicit context about *why* it exists or what happens to its output (contrast critic.py/pr_evaluator.py's module docstrings, which explain the downstream consumer) — a minor internal-documentation gap, not a model-facing one. |
| 3 | Structure & organization | 5 | Short, single-purpose, format-first — appropriate for a cheap, latency-sensitive call. |
| 4 | Output format specification | 5 | Strict JSON array, explicit per-field character caps (140 chars, `spec_clarification.py:41`), "no Markdown" stated twice (system prompt + user prompt) — a well-reinforced format constraint appropriate for direct parsing. |
| 5 | Examples & demonstrations | 4 | The shape example (`spec_clarification.py:36-39`) shows the exact JSON structure; no example of a *good vs. bad* question (e.g., contrasting "what's your timeline?" against a decision-shaping question) beyond the one line of prose guidance — a worked contrast pair would sharpen the "skip generic questions" instruction more than prose alone. |
| 6 | Edge case & failure handling | 4 | "If the statement is already crisp, return fewer — but never zero" (`spec_clarification.py:32`) is a clear floor. No stated ceiling behavior if the statement is *extremely* vague (could plausibly warrant more than 5 decision-shaping questions) — the prompt hard-caps at 5 with no escape valve, which is a deliberate product choice (module docstring: "the modal stays a conversation, not a survey," `spec_clarification.py:21`) rather than an oversight, so this doesn't lower the score further. |
| 7 | Constraint completeness | 4 | Bounded, sensible constraints throughout. No stated behavior for a problem statement that is empty or near-empty (e.g., a two-word idea) — plausible given `workspace.problem_statement` is likely required non-empty upstream, but the prompt itself doesn't defensively address it. |
| 8 | Injection & untrusted-content safety | **2** | This is the **one prompt in the entire family that imports no `SECURITY_AND_PRIVACY_RULES`** (`spec_clarification.py:16` imports only `wrap_untrusted_content`, not the security-rules constant) and whose user prompt (`spec_clarification.py:46-60`) contains **no explicit "ignore embedded instructions / this is data not instructions" sentence** — every sibling prompt (spec.py, plan.py, harness.py, tasks.py, demo_day.py, harness_patch.py, increment_service.py, critic.py, pr_evaluator.py) either imports the shared security rules or states the ignore-injection instruction inline, or both. `wrap_untrusted_content` is used (`spec_clarification.py:53`), so there is *some* structural defense, but the authority-hierarchy framing that tells the model *what to do* when it encounters conflicting instructions inside the wrapped block is absent here alone. |
| 9 | Variable interpolation safety | 4 | Single wrapped variable, no bespoke parsing logic to introduce a new failure mode. |
| 10 | Consistency with target model | 3 | The module docstring names the intended judge models as "Claude Haiku / GPT-4o Mini / Gemini Flash" (`spec_clarification.py:8`) — **stale**: per CLAUDE.md and `model_catalog.py`'s current tier ladder, the live judge-tier defaults are Haiku 4.5 / GPT-5.4 Mini / Gemini 3.5 Flash, and GPT-4o Mini is not a family this codebase's current catalog appears to target as the active judge default. Comment-only drift (doesn't affect behavior — actual routing comes from `JUDGE_MODELS`/`model_catalog.py`, not this docstring), but it's exactly the kind of stale in-code claim that misleads a future maintainer reading this file in isolation. |
| 11 | Token & cost efficiency | 5 | Very lean — appropriate for a cheap, sub-cent, latency-sensitive call per its own stated design goal. |
| 12 | Determinism & robustness | 4 | The 3-5 bound plus strict-JSON-only framing reduces format variance; caller (`spec_clarifier.py`) treats any parse failure as an empty list and silently bypasses the modal (`spec_clarifier.py:209-219`), a sensible fail-open design that limits the blast radius of any residual non-determinism. |
| 13 | Maintainability | 3 | No `load_prompt`/Langfuse hook and no version constant (contrast every other stage prompt, which has both) — this is the only judge-family prompt with zero version tracking, so a silent edit to this file is invisible to the telemetry/cost-ledger `prompt_version` mechanism the rest of the codebase relies on. |
| 14 | Testability & eval coverage | 3 | No golden-corpus entry or `harness/prompt_eval` grader specifically targets clarification-question quality (the golden_prompts directory has route/compression/grounding corpora but nothing named for clarification) — the fail-open design (dimension 12) reduces the *blast radius* of an undetected regression here, but doesn't eliminate the coverage gap. |

**Top risk:** Dimension 8 — this is the only prompt in the codebase's own documented threat model ("every prompt... including refinement instructions... treat as untrusted") that doesn't carry the authority-hierarchy defense the team applies everywhere else. The practical blast radius is bounded (output is 3-5 short questions a human reads and answers before they reach any downstream artifact), but it's a real, evidenced inconsistency in a security-conscious codebase.

**Verdict:** HIGH.
