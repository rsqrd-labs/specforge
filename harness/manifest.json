{
  "name": "specforge-v1-harness",
  "version": "1.0.0",
  "pipeline_order": ["spec", "plan", "harness", "tasks"],
  "purpose": "Executable red/green contract for building SpecForge V1 with agentic coding tasks.",
  "commands": {
    "backend": "cd backend && uv run pytest ../harness/tests/backend",
    "frontend": "cd frontend && pnpm vitest run ../harness/tests/frontend",
    "e2e": "cd frontend && pnpm playwright test ../harness/tests/e2e",
    "schemas": "for f in harness/schemas/*.schema.json; do python3 -m json.tool \"$f\" > /dev/null || exit 1; done"
  },
  "coverage_targets": {
    "backend_services": 80,
    "harness_requirement_coverage": 80,
    "task_harness_reference_validity": 100
  },
  "test_groups": [
    {
      "id": "structure",
      "path": "harness/tests/backend/test_project_structure.py",
      "proves": "The monorepo, backend, frontend, docker, and environment skeleton match the V1 plan."
    },
    {
      "id": "backend-contracts",
      "path": "harness/tests/backend",
      "proves": "FastAPI app factory, health, data models, routers, stage lifecycle, credits, security, evals, and export behavior match the spec."
    },
    {
      "id": "security-audit-contracts",
      "path": "harness/tests/backend/test_security_audit_contract.py",
      "proves": "The post-audit security fixes block Zip Slip exports, bound refine inputs, validate selected text, trust proxy headers safely, emit security headers, and keep dev datastores off public interfaces."
    },
    {
      "id": "frontend-contracts",
      "path": "harness/tests/frontend",
      "proves": "Strict TypeScript contracts, auth token handling, API client refresh, stores, and workspace UI state match the spec."
    },
    {
      "id": "provider-agnostic-llm-cost-contracts",
      "path": "harness/tests/backend/test_phase12_llm_cost_contract.py",
      "proves": "Phase 12 provider-agnostic LLM cost controls: capability registry, tier routing, prompt reuse, normalized telemetry, cache keys, and explicit cross-provider fallback."
    },
    {
      "id": "github-integration-contracts",
      "path": "harness/tests/backend/test_phase13_github_integration_contract.py",
      "proves": "Phase 13 GitHub export integration: ORM models, Fernet token storage, GitHubAPIClient typed exceptions, task parser purity, export orchestrator, router error mapping, rate-limit tier, and CI wiring."
    },
    {
      "id": "github-integration-frontend-contracts",
      "path": "harness/tests/frontend/phase13-github-integration.contract.test.ts",
      "proves": "Phase 13 frontend layer: TypeScript interfaces, api.ts GitHub functions (404 safety), Settings page route, ExportGitHubModal four-phase UI, workspace header GitHub button, and index.css design-system classes."
    },
    {
      "id": "v1-3-usefulness-contracts",
      "path": "harness/tests/backend/test_phase14_v13_usefulness_contract.py",
      "proves": "Phase 14 V1.3 usefulness improvements (backend): two new migrations (workspace v1.3 fields + templates table), Workspace + Template ORM models, Spec Clarification judge-model service with sanitisation and best-effort timeout, task Priority/Estimate enforcement in the tasks prompt + online eval, WeasyPrint PDF export service with no-network resource fetcher, public share service with CSPRNG slug generation and unambiguous alphabet, unauthenticated /public/{slug} router with allow-list response shape and noindex headers, unauthenticated /templates router backed by an idempotent seed script, harness coverage figure surfaced on the workspace response, and preservation of the pre-existing ZIP / GitHub export paths and the gap_patch_used regen gate."
    },
    {
      "id": "v1-3-usefulness-frontend-contracts",
      "path": "harness/tests/frontend/phase14-v13-usefulness.contract.test.ts",
      "proves": "Phase 14 V1.3 usefulness improvements (frontend): api.ts adds the eight v1.3 client functions without removing prior exports, SpecClarificationModal with Skip / Use-answers paths wired into Workspace, parseEffortSummary helper with graceful null on missing block plus workspace header chip, ExportPDFButton triggering a real browser download, SharePublicLinkModal with copy/toggle/rotate controls plus PublicWorkspaceView at /p/:slug outside the auth guard with noindex meta and no authenticated-store imports, robots.txt Disallow /p/, TemplatesStrip on Dashboard and the workspace creation form with template_slug provenance, harness-coverage chip rendered in workspace header / dashboard card / public view, plus the design-system CSS classes for each component."
    },
    {
      "id": "e2e-smoke",
      "path": "harness/tests/e2e/specforge-smoke.spec.ts",
      "proves": "The core workspace pipeline can be completed through the browser once services are running."
    }
  ],
  "agent_rules": [
    "Do not skip or reorder stages: SPEC -> PLAN -> HARNESS -> TASKS.",
    "Every implementation task must name at least one harness test that turns green.",
    "Prefer adding production code over weakening harness tests.",
    "If a harness expectation conflicts with a newer spec decision, update the manifest and test together."
  ]
}
