Build a collaborative planning board where authenticated team members create
cards, move cards between columns, receive live updates, and replay missed
events after reconnecting.

The product must enforce authentication, authorization, input validation, rate
limit controls, audit logging, and secret-safe diagnostics.
