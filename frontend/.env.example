VITE_API_URL=http://localhost:8000
VITE_SENTRY_DSN=
# Dark-launch the unified branded loader (issue #21). Set to "true" to enable
# <BrandLoader> on every migrated surface; any other value keeps it off.
VITE_BRANDED_LOADERS=
