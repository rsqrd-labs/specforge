# SEO remediation plan T-2.5 / issue #18: as of the Phase 3 domain cutover
# (2026-07-26), thought2build.com and www are owned by the marketing zone
# (apps/marketing/public robots policy is authoritative there). This file only
# governs direct hits on the SPA's raw deployment host
# (thought2build.vercel.app) — apex/www never serve this file, since the
# marketing zone's own root-level robots.txt.ts endpoint answers /robots.txt
# on those hosts directly (it is not one of the proxied rewrite paths in
# apps/marketing/vercel.json). The raw deployment host has no reason to be
# crawled or indexed at all, so block it outright rather than special-casing
# /p/ and /sb/ here.
User-agent: *
Disallow: /
